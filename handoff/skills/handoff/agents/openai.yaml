interface:
  display_name: "Handoff"
  short_description: "Work-closeout automation for the next worker."
  default_prompt: "Use $handoff to wrap up this session and write a handoff for the next worker."

policy:
  allow_implicit_invocation: true
